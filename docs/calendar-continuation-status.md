# 캘린더 작업 이어받기 — 2026-08-31

## 최신: 기존 로그인 유지로 분리 완료

새 인증 전환은 사용자 요청으로 보류했다. 로컬 로그인·가입·프로필 변경은 기존 방식으로
복원하고 캘린더·모집·당직 변경은 유지했다. 새 서버/화면/전환 SQL은 프로젝트 내부
backups/deferred-auth-2026-08-31T08-13-13-297Z에 보관했다.
상세 분리 범위와 검증은 docs/legacy-login-separation.md를 따른다.
앞서 백업(약 46초) 및 계정 테이블 복원(약 22초)은 성공했고, 승인된 미연결 Auth 12개는
삭제 완료했다. 회원 278개 및 게스트/임시 12명은 보존했다. 아래 실패 기록은 과거 이력이다.
이번 분리 과정에서는 운영 DB와 배포를 변경하지 않았다.

## 최신 이어받기: `캘린더 개편 작업 이어가기 (2)` 이후

- 최신 사용자 피드백: 로그인 전환이 지나치게 오래 걸리고 입력 반복이 비효율적이라고 지적했다.
  추가 암호 입력 GUI 개발은 중단했으며, 그 과정에서 시작한 미완성 변경도 되돌렸다.
  기존에 검증한 백업 도구는 유지한다. 자동 재시도나 새 입력 창은 열지 않았다.
- 사용자의 후속 실행은 연결 검사를 통과한 뒤 `recovery-key` 단계에서 멈췄다.
  제공한 화면의 오류는 `passphrases didn't match`다. 실제 백업 성공으로 취급하지 않는다.
  백업 외에도 계정 연결/소유권 예외 처리 등 운영 전환 조건이 남아 있음을 먼저 설명해야 한다.

- 해당 작업 ID는 `01a056a0-7b7f-7e51-97a2-dc9f2e75e3cb`다. 마지막 작업은 캘린더 UI가
  아니라 인증 전환 전 암호화 백업의 `connection / readonly-check` 실패 수정이었다.
- 사용자 DB 비밀번호는 다시 요청/조회하지 않았다. Supavisor의 startup 옵션 처리와
  실제 트랜잭션의 읽기 전용 상태를 구분하도록 백업 연결 검사를 수정했다.
- 큰 파일에서 목차 도구가 먼저 끝나도 전체 복호화·바이트·해시 검증을 계속하도록 수정했다.
  상세 근거와 가상 DB 검증 범위는 `docs/account-auth-backup-readiness.md`에 기록했다.
- 다음 사용자 단계는 수정한 로컬 입력 창에서 직접 암호를 입력해 재시도하는 것이다.
  실제 백업 성공은 새 `status.json`으로 확인해야 하며, 별도의 복구 검증도 필요하다.
- 이번 이어받기에서는 운영 DB·계정·원본 기록·캘린더 화면·배포 상태를 변경하지 않았다.

## 이번 작업

- 이전 `캘린더 개편 계획 이어가기` 대화의 마지막 요청은 프로그램 작성의 날짜·시간·소요 시간
  한 줄 배치, 모집 기간의 같은 입력 디자인 적용이었다. 이 UI 변경을 로컬에 반영했다.
- 기존 당직 검색·선택 및 두 화면의 같은 날짜별 당직 사용 코드를 보존했다.
  추가한 `scripts/test-duty-roster.mjs`로 동명이인, 동시 등록, 오래된 값 덮어쓰기 차단을 검증했다.
- 실제 DB·스키마·원본 로그·사용자 계정은 변경하지 않았다. 배포하지 않았다.

## 로컬 검증

- 캘린더 규칙, 색상, 캘린더 DB, 프로그램 공개 범위, 당직 충돌 테스트 5개가 통과했다.
- `scratch/calendar-integration.html`의 실제 작성 폼과 가상 응답으로 1440px 데스크톱
  한 줄 배치 및 390px 모바일 줄바꿈/가로 넘침 없음을 확인했다.
- 가상 저장에서 프로그램 14:07, 모집 시작 09:03, 모집 종료 12:59의 한국 시간 변환을 확인했다.
  날짜를 비우고 시간을 먼저 오전 12:01로 선택한 뒤 날짜를 넣어도 자정 00:01로 정확히 저장된다.
  이 검사는 운영 DB나 실제 신청/알림으로 전송되지 않는다.
- 전체 빌드 통과. 기존 대형 번들·오래된 브라우저 호환 목록 경고는 남아 있다.

## 중단 진단: 확인된 사실

이전 작업 ID: `01a05642-40ce-7e31-aaca-ead73713a45e`.
해당 로컬 작업 기록의 종료 이벤트에 다음 두 오류가 있다. 시각은 한국 시간이다.

| 시각 | 당시 요청 | 종료 오류 |
| --- | --- | --- |
| 2026-08-31 14:54:11 | 당직 스태프 검색·선택 및 두 화면 충돌 처리 | `misalignment_policy_violation` |
| 2026-08-31 14:55:41 | 프로그램 작성 일정/모집 입력 디자인 정리 | `misalignment_policy_violation` |

공통 메시지는 `This request was blocked by our safety systems.`다.
두 번째 요청은 약 3.8초 뒤 도구 실행 없이 종료되었다.
이 두 종료 이벤트는 앱 소스의 빌드 오류나 로컬 서버 접속 실패가 아니라 서비스 측 안전 차단을 가리킨다.
로그에는 구체적인 판정 근거나 원인이 된 문장/파일이 없다. 특정 단어, 이미지, 모델,
플러그인 또는 사용량을 원인으로 단정할 수 없다.

앞선 대화에는 로그인 보안 전환·계정 권한·비밀번호 초기화 작업도 함께 있었다.
이러한 문맥이 판정에 영향을 주었을 가능성은 있지만 **확인되지 않은 가설**이다.
마지막 UI 요청 자체의 오탐 가능성도 배제할 수 없다.

OpenAI 공식 문서는 정당한 보안 작업 또는 무관한 작업도 안전 장치에 걸릴 수 있다고 설명하고,
사용자 안내와 요청 로그를 확인한 후 가능한 경우 `/feedback`으로 오탐을 신고하도록 안내한다.
출처: [Models and Trusted Access — False positives](https://learn.chatgpt.com/docs/cyber-safety#false-positives).
지원 문의 시 위 작업 ID, 시각, 오류 코드, 당시 UI 변경 요청을 제공하면 된다.
전체 로그에는 민감 정보가 포함될 수 있으므로 비밀번호·토큰·회원 정보가 없는 최소 오류 기록만 공유한다.
이번 작업에서는 안전 설정을 해제하거나 변경하지 않았다.

## 별개로 확인된 로컬 실행 문제

- 이번 작업에서 개발 서버 실행 시 `spawn EPERM`이 발생했다. 제한된 실행 환경에서
  하위 프로세스 실행이 거절된 것으로, 승인된 방식으로 재실행하자 5173 포트가 정상 기동했다.
- 이전 대화의 로컬 로그인 오류는 아직 배포하지 않은 인증 서버에 화면을 먼저 연결한 문제가 보고돼 있었다.
  새 인증 서버·DB 전환은 완료된 것으로 간주하면 안 된다.
- `docs/account-auth-transition.md`의 경고대로 현재 변경 전체를 Firebase만 단독 배포하면
  로그인 장애 위험이 있다. 운영 인증 전환은 별도 검토·승인이 필요하다.
- 후속 점검에서 운영 `account-auth`의 HTTP 404를 확인했다. 로컬 Hosting 설정에
  인증 서버 준비 상태를 확인하는 사전 배포 차단 장치를 추가했으므로 기본 배포 명령은 준비 전 중단된다.
  이는 장애 유발 배포의 예방이며 운영 인증 전환이 완료되었다는 뜻은 아니다.
